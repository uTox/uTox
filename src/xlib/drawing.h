#ifndef XLIB_DRAWING_H
#define XLIB_DRAWING_H

#include "window.h"

#include <stdbool.h>

bool draw_window_set(struct xlib_window *new_win);

/* List of common functions are kept in main.h */

#endif
